[project]
name = "hexlet-code"
version = "0.1.0"
description = "Page Analyzer"
dependencies = [
    "flask",
    "gunicorn", 
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hexlet_code"]
